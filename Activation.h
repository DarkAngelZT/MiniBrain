#pragma once
#include "Node.h"

namespace MiniBrain{
    class Activation: public Node
    {
    private:
        /* data */
    public:
        Activation(/* args */) {}
        ~Activation() {}
    };
}